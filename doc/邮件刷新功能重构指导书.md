# 邮件刷新功能重构指导书（精确版）

## 文档信息

- **版本**：2.0
- **创建日期**：2026-04-16
- **更新日期**：2026-04-16
- **状态**：待审核
- **目标**：将邮件刷新逻辑从自动触发改为用户手动触发

---

## 1. 需求回顾

### 1.1 核心需求

1. **手动刷新原则**：切换到邮箱界面时不自动刷新，只有点击刷新按钮才执行刷新操作
2. **单账户刷新流程**：检索账号 → 清空数据库 → 读取服务器 → 存入数据库 → 更新UI
3. **全部账户刷新流程**：遍历所有账号 → 分别刷新 → 合并到AllEmail → 按时间排序（新的在前）

### 1.2 需要修改的文件清单

| 序号 | 文件路径 | 修改类型 | 优先级 |
|------|----------|----------|--------|
| 1 | ResourceCode/email/EmailBoxWindow.h | 修改函数声明 | P0 |
| 2 | ResourceCode/email/EmailBoxWindow.cpp | 重构函数实现 | P0 |
| 3 | ResourceCode/email/EmailDataManager.h | 新增函数声明 | P0 |
| 4 | ResourceCode/email/EmailDataManager.cpp | 新增函数实现 | P0 |

---

## 2. EmailDataManager 重构（精确操作）

### 2.1 EmailDataManager.h 修改

**文件路径**：`ResourceCode/email/EmailDataManager.h`

#### 2.1.1 在类声明末尾添加新方法

**找到位置**：在 `private:` 区域之前添加以下声明

**操作步骤**：

1. 打开文件 `ResourceCode/email/EmailDataManager.h`
2. 找到类声明中的 `private:` 关键字（第59行附近）
3. 在 `private:` 之前插入以下新方法声明

**添加的代码**：

```cpp
    // ==================== EmailBox 刷新管理方法 ====================
    
    // 功能：准备指定账户的EmailBox进行刷新（清空并重建）
    // 参数：emailAddress - 邮箱地址（将用于构建数据库文件名）
    // 返回：true-成功，false-失败
    // 调用时机：单账户刷新时调用
    bool prepareEmailBoxForRefresh(const QString& emailAddress);
    
    // 功能：从指定账户的EmailBox加载邮件列表
    // 参数：emailAddress - 邮箱地址
    // 返回：邮件列表（按received_at降序）
    // 调用时机：加载显示邮件时调用
    QVector<Email> loadEmailsFromEmailBox(const QString& emailAddress);
    
    // 功能：准备AllEmailBox进行刷新（清空并重建）
    // 返回：true-成功，false-失败
    // 调用时机：全部账户刷新时调用
    bool prepareAllEmailBoxForRefresh();
    
    // 功能：合并所有账户的EmailBox到AllEmailBox
    // 参数：accounts - 所有邮箱账户列表
    // 返回：true-成功，false-失败
    // 说明：会自动按received_at降序排序
    // 调用时机：全部账户刷新完成后调用
    bool mergeAllEmailBoxesToAllEmailBox(const QVector<EmailAccount>& accounts);
    
    // 功能：从AllEmailBox加载邮件列表
    // 返回：邮件列表（按received_at降序）
    // 调用时机：加载显示"全部账户"邮件时调用
    QVector<Email> loadEmailsFromAllEmailBox();
    
    // ==================== 原有方法（保留但标记为内部使用）====================
    
    // 以下方法保留为内部实现，不再对外暴露
    bool clearEmailBoxForAccount(const QString& emailAddress);
    bool createEmailBoxForAccount(const QString& emailAddress);
    QVector<Email> getEmailsFromEmailBox(const QString& emailAddress);
```

### 2.2 EmailDataManager.cpp 修改

**文件路径**：`ResourceCode/email/EmailDataManager.cpp`

#### 2.2.1 实现 prepareEmailBoxForRefresh()

**操作步骤**：

1. 打开文件 `ResourceCode/email/EmailDataManager.cpp`
2. 在文件末尾（最后一个方法之后）添加新方法实现
3. 如果 `clearEmailBoxForAccount()` 和 `createEmailBoxForAccount()` 已存在，可直接复用

**添加的代码**：

```cpp
bool EmailDataManager::prepareEmailBoxForRefresh(const QString& emailAddress) {
    qDebug() << "[EmailDataManager] prepareEmailBoxForRefresh:" << emailAddress;
    
    // 步骤1：清空现有的EmailBox
    if (!clearEmailBoxForAccount(emailAddress)) {
        qDebug() << "[EmailDataManager] Failed to clear EmailBox for:" << emailAddress;
        return false;
    }
    
    // 步骤2：创建新的空EmailBox（确保数据库结构存在）
    if (!createEmailBoxForAccount(emailAddress)) {
        qDebug() << "[EmailDataManager] Failed to create EmailBox for:" << emailAddress;
        return false;
    }
    
    qDebug() << "[EmailDataManager] EmailBox prepared successfully for:" << emailAddress;
    return true;
}
```

#### 2.2.2 实现 loadEmailsFromEmailBox()

**操作步骤**：

1. 在 `prepareEmailBoxForRefresh()` 方法之后添加此方法
2. 逻辑：打开数据库 → 查询 → 解析 → 关闭 → 返回

**添加的代码**：

```cpp
QVector<Email> EmailDataManager::loadEmailsFromEmailBox(const QString& emailAddress) {
    qDebug() << "[EmailDataManager] loadEmailsFromEmailBox:" << emailAddress;
    
    // 步骤1：构建数据库文件路径
    QString appDataPath = QStandardPaths::writableLocation(QStandardPaths::AppDataLocation);
    QString dbFileName = QString("/%1_EmailBox.db").arg(emailAddress);
    QString dbPath = appDataPath + dbFileName;
    
    // 步骤2：检查数据库文件是否存在
    if (!QFile::exists(dbPath)) {
        qDebug() << "[EmailDataManager] EmailBox.db does not exist:" << dbPath;
        return QVector<Email>();
    }
    
    // 步骤3：打开数据库连接（使用独立的连接名避免冲突）
    QString connectionName = QString("email_box_load_%1").arg(qHash(emailAddress));
    QSqlDatabase db = QSqlDatabase::addDatabase("QSQLITE", connectionName);
    db.setDatabaseName(dbPath);
    
    if (!db.open()) {
        qDebug() << "[EmailDataManager] Failed to open database:" << dbPath;
        QSqlDatabase::removeDatabase(connectionName);
        return QVector<Email>();
    }
    
    // 步骤4：执行查询（按接收时间降序，即新的在前）
    QSqlQuery query(db);
    query.prepare("SELECT * FROM emails WHERE folder = :folder ORDER BY received_at DESC");
    query.bindValue(":folder", static_cast<int>(EmailFolder::Inbox));
    
    QVector<Email> emails;
    if (query.exec()) {
        while (query.next()) {
            emails.append(parseEmailFromQuery(query));
        }
    } else {
        qDebug() << "[EmailDataManager] Query failed:" << query.lastError().text();
    }
    
    // 步骤5：关闭数据库连接
    db.close();
    QSqlDatabase::removeDatabase(connectionName);
    
    qDebug() << "[EmailDataManager] Loaded" << emails.size() << "emails from:" << emailAddress;
    return emails;
}
```

#### 2.2.3 实现 prepareAllEmailBoxForRefresh()

**操作步骤**：

1. 在 `loadEmailsFromEmailBox()` 方法之后添加此方法
2. 逻辑：删除旧文件 → 创建新数据库结构

**添加的代码**：

```cpp
bool EmailDataManager::prepareAllEmailBoxForRefresh() {
    qDebug() << "[EmailDataManager] prepareAllEmailBoxForRefresh";
    
    // 步骤1：构建AllEmailBox数据库路径
    QString appDataPath = QStandardPaths::writableLocation(QStandardPaths::AppDataLocation);
    QString allEmailBoxPath = appDataPath + "/AllEmailBox.db";
    
    // 步骤2：关闭并移除现有的数据库连接
    QString connectionName = "all_email_box_refresh";
    if (QSqlDatabase::contains(connectionName)) {
        QSqlDatabase db = QSqlDatabase::database(connectionName);
        if (db.isOpen()) {
            db.close();
        }
        QSqlDatabase::removeDatabase(connectionName);
    }
    
    // 步骤3：如果数据库文件存在，则删除（清空）
    QFile dbFile(allEmailBoxPath);
    if (dbFile.exists()) {
        if (!dbFile.remove()) {
            qDebug() << "[EmailDataManager] Failed to remove existing AllEmailBox.db";
            return false;
        }
        qDebug() << "[EmailDataManager] Removed existing AllEmailBox.db";
    }
    
    // 步骤4：创建新的空数据库结构
    return createAllEmailBoxDatabase();
}

bool EmailDataManager::createAllEmailBoxDatabase() {
    qDebug() << "[EmailDataManager] createAllEmailBoxDatabase";
    
    // 步骤1：构建数据库路径
    QString appDataPath = QStandardPaths::writableLocation(QStandardPaths::AppDataLocation);
    QDir appDir(appDataPath);
    if (!appDir.exists()) {
        if (!appDir.mkpath(".")) {
            qDebug() << "[EmailDataManager] Failed to create directory:" << appDataPath;
            return false;
        }
    }
    
    QString allEmailBoxPath = appDataPath + "/AllEmailBox.db";
    
    // 步骤2：创建数据库连接
    QString connectionName = "all_email_box_refresh";
    QSqlDatabase db = QSqlDatabase::addDatabase("QSQLITE", connectionName);
    db.setDatabaseName(allEmailBoxPath);
    
    if (!db.open()) {
        qDebug() << "[EmailDataManager] Failed to open database:" << allEmailBoxPath;
        QSqlDatabase::removeDatabase(connectionName);
        return false;
    }
    
    // 步骤3：创建数据表（与EmailBox相同的结构）
    QString createTableSql = R"(
        CREATE TABLE IF NOT EXISTS emails (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            account_id INTEGER,
            message_id TEXT,
            subject TEXT,
            from_address TEXT,
            from_name TEXT,
            to_address TEXT,
            cc_addresses TEXT,
            sent_at DATETIME,
            received_at DATETIME,
            body TEXT,
            html_body TEXT,
            is_read INTEGER DEFAULT 0,
            is_starred INTEGER DEFAULT 0,
            folder INTEGER DEFAULT 0,
            raw_json TEXT,
            created_at DATETIME,
            modified_at DATETIME
        )
    )";
    
    QSqlQuery query(db);
    if (!query.exec(createTableSql)) {
        qDebug() << "[EmailDataManager] Failed to create table:" << query.lastError().text();
        db.close();
        QSqlDatabase::removeDatabase(connectionName);
        return false;
    }
    
    // 步骤4：创建索引以加速查询
    query.exec("CREATE INDEX IF NOT EXISTS idx_emails_folder ON emails(folder)");
    query.exec("CREATE INDEX IF NOT EXISTS idx_emails_received_at ON emails(received_at)");
    
    db.close();
    QSqlDatabase::removeDatabase(connectionName);
    
    qDebug() << "[EmailDataManager] AllEmailBox.db created successfully";
    return true;
}
```

#### 2.2.4 实现 mergeAllEmailBoxesToAllEmailBox()

**操作步骤**：

1. 在 `createAllEmailBoxDatabase()` 方法之后添加此方法
2. 逻辑：准备AllEmailBox → 遍历账户 → 复制邮件 → 排序

**添加的代码**：

```cpp
bool EmailDataManager::mergeAllEmailBoxesToAllEmailBox(const QVector<EmailAccount>& accounts) {
    qDebug() << "[EmailDataManager] mergeAllEmailBoxesToAllEmailBox, accounts:" << accounts.size();
    
    // 步骤1：准备AllEmailBox（清空并创建）
    if (!prepareAllEmailBoxForRefresh()) {
        qDebug() << "[EmailDataManager] Failed to prepare AllEmailBox";
        return false;
    }
    
    // 步骤2：打开AllEmailBox数据库
    QString appDataPath = QStandardPaths::writableLocation(QStandardPaths::AppDataLocation);
    QString allEmailBoxPath = appDataPath + "/AllEmailBox.db";
    
    QString connectionName = "all_email_box_merge";
    QSqlDatabase allEmailDb = QSqlDatabase::addDatabase("QSQLITE", connectionName);
    allEmailDb.setDatabaseName(allEmailBoxPath);
    
    if (!allEmailDb.open()) {
        qDebug() << "[EmailDataManager] Failed to open AllEmailBox";
        return false;
    }
    
    // 步骤3：开启事务以提高性能
    allEmailDb.transaction();
    
    // 步骤4：遍历所有账户，复制邮件到AllEmailBox
    int totalMerged = 0;
    for (const EmailAccount& account : accounts) {
        qDebug() << "[EmailDataManager] Merging emails from:" << account.emailAddress;
        
        // 从该账户的EmailBox加载邮件
        QVector<Email> accountEmails = loadEmailsFromEmailBox(account.emailAddress);
        
        // 复制每封邮件到AllEmailBox
        for (const Email& email : accountEmails) {
            // 创建新的Email对象（id设为0，由数据库自动生成）
            Email mergedEmail = email;
            mergedEmail.id = 0;
            mergedEmail.accountId = account.id;
            
            // 插入到AllEmailBox
            QSqlQuery insertQuery(allEmailDb);
            insertQuery.prepare(R"(
                INSERT INTO emails (
                    account_id, message_id, subject, from_address, from_name,
                    to_address, cc_addresses, sent_at, received_at, body,
                    html_body, is_read, is_starred, folder, raw_json,
                    created_at, modified_at
                ) VALUES (
                    :account_id, :message_id, :subject, :from_address, :from_name,
                    :to_address, :cc_addresses, :sent_at, :received_at, :body,
                    :html_body, :is_read, :is_starred, :folder, :raw_json,
                    :created_at, :modified_at
                )
            )");
            
            insertQuery.bindValue(":account_id", mergedEmail.accountId);
            insertQuery.bindValue(":message_id", mergedEmail.messageId);
            insertQuery.bindValue(":subject", mergedEmail.subject);
            insertQuery.bindValue(":from_address", mergedEmail.fromAddress);
            insertQuery.bindValue(":from_name", mergedEmail.fromName);
            insertQuery.bindValue(":to_address", mergedEmail.toAddress);
            insertQuery.bindValue(":cc_addresses", mergedEmail.ccAddresses.join(","));
            insertQuery.bindValue(":sent_at", mergedEmail.sentAt);
            insertQuery.bindValue(":received_at", mergedEmail.receivedAt);
            insertQuery.bindValue(":body", mergedEmail.body);
            insertQuery.bindValue(":html_body", mergedEmail.htmlBody);
            insertQuery.bindValue(":is_read", mergedEmail.isRead ? 1 : 0);
            insertQuery.bindValue(":is_starred", mergedEmail.isStarred ? 1 : 0);
            insertQuery.bindValue(":folder", static_cast<int>(mergedEmail.folder));
            insertQuery.bindValue(":raw_json", mergedEmail.rawJson);
            insertQuery.bindValue(":created_at", mergedEmail.createdAt);
            insertQuery.bindValue(":modified_at", mergedEmail.modifiedAt);
            
            if (!insertQuery.exec()) {
                qDebug() << "[EmailDataManager] Failed to insert email:" << insertQuery.lastError().text();
            } else {
                totalMerged++;
            }
        }
    }
    
    // 步骤5：提交事务
    allEmailDb.commit();
    allEmailDb.close();
    QSqlDatabase::removeDatabase(connectionName);
    
    qDebug() << "[EmailDataManager] Merged" << totalMerged << "emails to AllEmailBox";
    return true;
}
```

#### 2.2.5 实现 loadEmailsFromAllEmailBox()

**操作步骤**：

1. 在 `mergeAllEmailBoxesToAllEmailBox()` 方法之后添加此方法
2. 逻辑：打开数据库 → 查询 → 解析 → 返回

**添加的代码**：

```cpp
QVector<Email> EmailDataManager::loadEmailsFromAllEmailBox() {
    qDebug() << "[EmailDataManager] loadEmailsFromAllEmailBox";
    
    // 步骤1：构建数据库路径
    QString appDataPath = QStandardPaths::writableLocation(QStandardPaths::AppDataLocation);
    QString allEmailBoxPath = appDataPath + "/AllEmailBox.db";
    
    // 步骤2：检查数据库文件是否存在
    if (!QFile::exists(allEmailBoxPath)) {
        qDebug() << "[EmailDataManager] AllEmailBox.db does not exist";
        return QVector<Email>();
    }
    
    // 步骤3：打开数据库连接
    QString connectionName = "all_email_box_load";
    QSqlDatabase db = QSqlDatabase::addDatabase("QSQLITE", connectionName);
    db.setDatabaseName(allEmailBoxPath);
    
    if (!db.open()) {
        qDebug() << "[EmailDataManager] Failed to open AllEmailBox.db";
        QSqlDatabase::removeDatabase(connectionName);
        return QVector<Email>();
    }
    
    // 步骤4：执行查询（按接收时间降序，即新的在前）
    QSqlQuery query(db);
    query.prepare("SELECT * FROM emails WHERE folder = :folder ORDER BY received_at DESC");
    query.bindValue(":folder", static_cast<int>(EmailFolder::Inbox));
    
    QVector<Email> emails;
    if (query.exec()) {
        while (query.next()) {
            emails.append(parseEmailFromQuery(query));
        }
    } else {
        qDebug() << "[EmailDataManager] Query failed:" << query.lastError().text();
    }
    
    // 步骤5：关闭数据库连接
    db.close();
    QSqlDatabase::removeDatabase(connectionName);
    
    qDebug() << "[EmailDataManager] Loaded" << emails.size() << "emails from AllEmailBox";
    return emails;
}
```

---

## 3. EmailBoxWindow 重构（精确操作）

### 3.1 EmailBoxWindow.h 修改

**文件路径**：`ResourceCode/email/EmailBoxWindow.h`

#### 3.1.1 修改构造函数声明（可选）

**操作步骤**：

1. 打开文件 `ResourceCode/email/EmailBoxWindow.h`
2. 找到构造函数声明（第26行附近）
3. 无需修改，构造函数保持原样

#### 3.1.2 修改私有槽函数声明

**操作步骤**：

1. 找到 `private slots:` 区域（第37-52行）
2. 无需修改槽函数声明

#### 3.1.3 修改私有函数声明

**操作步骤**：

1. 找到 `private:` 区域（第57-84行）
2. 找到以下需要修改的函数声明并替换

**需要修改的函数**：

```cpp
// 修改前（第74-78行）：
void fetchEmailsFromServer();
void performConnection();

// 修改后：
void refreshSingleAccount(const EmailAccount& account);
void refreshAllAccounts();
```

**替换的代码**：

```cpp
    // ==================== 刷新相关函数 ====================
    void refreshSingleAccount(const EmailAccount& account);    // 单账户刷新
    void refreshAllAccounts();                                  // 全部账户刷新
    void loadLocalCachedEmails();                              // 加载本地缓存（不触发网络请求）
    
    // 以下函数标记为废弃，不再直接调用
    // void fetchEmailsFromServer();  // 已废弃，使用 refreshSingleAccount 替代
    // void performConnection();      // 已废弃，逻辑合并到 refreshSingleAccount
    // void fetchEmailsForAllAccounts(); // 已废弃，使用 refreshAllAccounts 替代
```

**完整替换操作**：

找到文件中的这一段代码：

```cpp
void fetchEmailsFromServer();
void performConnection();
void fetchEmailsForAllAccounts();
```

替换为：

```cpp
void refreshSingleAccount(const EmailAccount& account);
void refreshAllAccounts();
void loadLocalCachedEmails();
```

### 3.2 EmailBoxWindow.cpp 修改

**文件路径**：`ResourceCode/email/EmailBoxWindow.cpp`

#### 3.2.1 修改 showEvent() - 移除自动刷新

**当前代码位置**：第107-119行

**操作步骤**：

1. 打开文件 `ResourceCode/email/EmailBoxWindow.cpp`
2. 找到 `showEvent()` 方法（第107行开始）
3. 完全替换该方法

**替换前**：

```cpp
void EmailBoxWindow::showEvent(QShowEvent* event) {
    QWidget::showEvent(event);
    
    static bool initialLoadDone = false;
    
    if (!initialLoadDone && m_isInitialized) {
        initialLoadDone = true;
        
        if (m_pythonEmailClient->isConnected()) {
            m_pythonEmailClient->disconnect();
        }
        
        for (const EmailAccount& account : m_accounts) {
            m_emailDataManager->clearEmailBoxForAccount(account.emailAddress);
            m_emailDataManager->createEmailBoxForAccount(account.emailAddress);
        }
        
        qDebug() << "[EmailBoxWindow] showEvent: Fetching emails for all accounts";
        fetchEmailsForAllAccounts();
    }
}
```

**替换后**：

```cpp
void EmailBoxWindow::showEvent(QShowEvent* event) {
    QWidget::showEvent(event);
    
    // 如果已经初始化且已有邮件数据，不再执行任何操作
    if (m_isInitialized && !m_emailItems.isEmpty()) {
        return;
    }
    
    // 首次显示时，仅加载本地缓存的邮件（不触发网络请求）
    qDebug() << "[EmailBoxWindow] showEvent: Loading local cached emails";
    loadLocalCachedEmails();
}
```

#### 3.2.2 添加 loadLocalCachedEmails() 方法

**操作步骤**：

1. 在文件末尾（`fetchAndShowEmailBody()` 方法之后）添加新方法
2. 或者在 `showEvent()` 方法之后添加

**添加的代码**：

```cpp
void EmailBoxWindow::loadLocalCachedEmails() {
    qDebug() << "[EmailBoxWindow] loadLocalCachedEmails: currentAccount.id =" << m_currentAccount.id;
    
    // 如果当前账户为空（全部账户），尝试加载AllEmailBox
    if (m_currentAccount.id == 0) {
        qDebug() << "[EmailBoxWindow] loadLocalCachedEmails: Loading from AllEmailBox";
        
        QVector<Email> emails = m_emailDataManager->loadEmailsFromAllEmailBox();
        
        // 如果AllEmailBox为空（未刷新过），从各账户EmailBox合并
        if (emails.isEmpty()) {
            qDebug() << "[EmailBoxWindow] loadLocalCachedEmails: AllEmailBox is empty, merging from account boxes";
            QVector<Email> allEmails;
            for (const EmailAccount& account : m_accounts) {
                QVector<Email> accountEmails = m_emailDataManager->loadEmailsFromEmailBox(account.emailAddress);
                allEmails.append(accountEmails);
            }
            
            // 按接收时间降序排序（新的在前）
            std::sort(allEmails.begin(), allEmails.end(), [](const Email& a, const Email& b) {
                return a.receivedAt > b.receivedAt;
            });
            
            displayEmails(allEmails);
        } else {
            displayEmails(emails);
        }
    } else {
        // 加载指定账户的EmailBox
        qDebug() << "[EmailBoxWindow] loadLocalCachedEmails: Loading from account:" << m_currentAccount.emailAddress;
        QVector<Email> emails = m_emailDataManager->loadEmailsFromEmailBox(m_currentAccount.emailAddress);
        displayEmails(emails);
    }
}
```

#### 3.2.3 修改 onRefreshEmails() - 重构刷新入口

**当前代码位置**：第243-260行

**操作步骤**：

1. 找到 `onRefreshEmails()` 方法
2. 完全替换该方法

**替换前**：

```cpp
void EmailBoxWindow::onRefreshEmails() {
    if (m_accounts.isEmpty()) {
        QMessageBox::warning(this, "提示", "请先添加邮箱账户");
        return;
    }

    for (const EmailAccount& account : m_accounts) {
        m_emailDataManager->clearEmailBoxForAccount(account.emailAddress);
        m_emailDataManager->createEmailBoxForAccount(account.emailAddress);
    }

    if (m_currentAccount.id == 0) {
        fetchEmailsForAllAccounts();
    } else {
        fetchEmailsFromServer();
    }
}
```

**替换后**：

```cpp
void EmailBoxWindow::onRefreshEmails() {
    qDebug() << "[EmailBoxWindow] onRefreshEmails: currentAccount.id =" << m_currentAccount.id;
    
    // 步骤1：检查是否有账户
    if (m_accounts.isEmpty()) {
        QMessageBox::warning(this, "提示", "请先添加邮箱账户");
        return;
    }
    
    // 步骤2：检查是否正在刷新（防止重复触发）
    if (m_fetchStep != FetchStep::Idle) {
        QMessageBox::information(this, "提示", "正在刷新中，请稍候...");
        return;
    }
    
    // 步骤3：根据当前选中的账户执行刷新
    if (m_currentAccount.id == 0) {
        // 全部账户刷新
        qDebug() << "[EmailBoxWindow] onRefreshEmails: Refreshing all accounts";
        refreshAllAccounts();
    } else {
        // 单账户刷新
        qDebug() << "[EmailBoxWindow] onRefreshEmails: Refreshing single account:" << m_currentAccount.emailAddress;
        refreshSingleAccount(m_currentAccount);
    }
}
```

#### 3.2.4 添加 refreshSingleAccount() 方法

**操作步骤**：

1. 在 `onRefreshEmails()` 方法之后添加新方法
2. 或者在文件中搜索 `fetchEmailsFromServer()` 并在其位置替换

**添加的代码**：

```cpp
void EmailBoxWindow::refreshSingleAccount(const EmailAccount& account) {
    qDebug() << "[EmailBoxWindow] refreshSingleAccount:" << account.emailAddress;
    
    // 步骤1：更新当前账户信息
    m_currentAccount = account;
    
    // 步骤2：准备数据库（清空并创建EmailBox）
    if (!m_emailDataManager->prepareEmailBoxForRefresh(account.emailAddress)) {
        QMessageBox::warning(this, "错误", "无法准备邮箱数据库");
        return;
    }
    
    // 步骤3：设置UI状态
    m_refreshBtn->setEnabled(false);
    m_refreshBtn->setText("连接中...");
    
    // 步骤4：显示进度对话框
    m_progressDialog = new QProgressDialog("正在连接邮箱服务器...", "取消", 0, 100, this);
    m_progressDialog->setWindowModality(Qt::WindowModal);
    m_progressDialog->setValue(10);
    m_progressDialog->show();
    
    // 步骤5：设置线程
    setupImapThread();
    
    // 步骤6：延迟执行连接（避免阻塞UI）
    QTimer::singleShot(100, this, [this, account]() {
        qDebug() << "[EmailBoxWindow] refreshSingleAccount: Connecting to server...";
        
        bool connected = m_pythonEmailClient->connectToServer(
            account.imapServer,
            account.imapPort,
            account.emailAddress,
            account.password,
            true
        );
        
        if (!connected) {
            qDebug() << "[EmailBoxWindow] refreshSingleAccount: Connection failed";
            cleanupProgressDialog();
            QMessageBox::warning(this, "连接失败",
                QString("无法连接到邮箱服务器：%1\n\n请检查：\n"
                        "1. 网络连接是否正常\n"
                        "2. IMAP服务器地址是否正确\n"
                        "3. IMAP端口是否正确(通常为993)").arg(account.emailAddress));
            loadLocalCachedEmails();
            return;
        }
        
        // 步骤7：选择邮箱
        m_progressDialog->setValue(40);
        m_progressDialog->setLabelText("正在打开收件箱...");
        qApp->processEvents();
        
        if (!m_pythonEmailClient->selectMailbox("INBOX")) {
            qDebug() << "[EmailBoxWindow] refreshSingleAccount: Failed to select mailbox";
            cleanupProgressDialog();
            m_pythonEmailClient->disconnect();
            QMessageBox::warning(this, "错误",
                QString("无法打开收件箱：%1").arg(account.emailAddress));
            loadLocalCachedEmails();
            return;
        }
        
        // 步骤8：获取邮件列表
        m_progressDialog->setValue(60);
        m_progressDialog->setLabelText("正在获取邮件列表...");
        qApp->processEvents();
        
        QString jsonResult = m_pythonEmailClient->fetchEmailListAsJsonFast(1, 100);
        
        // 步骤9：保存到数据库
        m_progressDialog->setValue(80);
        m_progressDialog->setLabelText("正在保存邮件...");
        qApp->processEvents();
        
        saveFetchedEmailsFromJson(jsonResult);
        
        // 步骤10：断开连接
        m_pythonEmailClient->disconnect();
        
        // 步骤11：更新UI
        m_progressDialog->setValue(95);
        m_progressDialog->setLabelText("正在刷新界面...");
        qApp->processEvents();
        
        loadEmailsFromAccountEmailBox();
        
        // 步骤12：清理
        cleanupProgressDialog();
        
        qDebug() << "[EmailBoxWindow] refreshSingleAccount: Completed";
    });
}
```

#### 3.2.5 添加 refreshAllAccounts() 方法

**操作步骤**：

1. 在 `refreshSingleAccount()` 方法之后添加新方法
2. 或者找到 `fetchEmailsForAllAccounts()` 并替换

**添加的代码**：

```cpp
void EmailBoxWindow::refreshAllAccounts() {
    qDebug() << "[EmailBoxWindow] refreshAllAccounts: Starting, accounts count:" << m_accounts.size();
    
    // 步骤1：设置UI状态
    m_refreshBtn->setEnabled(false);
    m_refreshBtn->setText("加载中...");
    
    // 步骤2：显示进度对话框
    m_progressDialog = new QProgressDialog("正在刷新所有邮箱...", "取消", 0, 100, this);
    m_progressDialog->setWindowModality(Qt::WindowModal);
    m_progressDialog->setValue(5);
    m_progressDialog->show();
    
    // 步骤3：遍历所有账户，分别刷新
    int totalAccounts = m_accounts.size();
    int currentAccountIndex = 0;
    
    for (const EmailAccount& account : m_accounts) {
        // 检查是否取消
        if (m_progressDialog->wasCanceled()) {
            qDebug() << "[EmailBoxWindow] refreshAllAccounts: User cancelled";
            break;
        }
        
        currentAccountIndex++;
        
        // 计算进度：每个账户占 80/totalAccounts，合并占 20
        int progress = 5 + (currentAccountIndex * 80) / totalAccounts;
        m_progressDialog->setValue(progress);
        m_progressDialog->setLabelText(QString("正在刷新 %1/%2: %3")
            .arg(currentAccountIndex)
            .arg(totalAccounts)
            .arg(account.emailAddress));
        qApp->processEvents();
        
        qDebug() << "[EmailBoxWindow] refreshAllAccounts: Processing account:" << account.emailAddress;
        
        // 步骤3a：准备该账户的数据库
        m_emailDataManager->prepareEmailBoxForRefresh(account.emailAddress);
        
        // 步骤3b：断开之前的连接
        if (m_pythonEmailClient->isConnected()) {
            m_pythonEmailClient->disconnect();
        }
        
        // 步骤3c：连接到服务器
        bool connected = m_pythonEmailClient->connectToServer(
            account.imapServer,
            account.imapPort,
            account.emailAddress,
            account.password,
            true
        );
        
        if (!connected) {
            qDebug() << "[EmailBoxWindow] refreshAllAccounts: Failed to connect to:" << account.emailAddress;
            continue;
        }
        
        // 步骤3d：选择邮箱
        if (!m_pythonEmailClient->selectMailbox("INBOX")) {
            qDebug() << "[EmailBoxWindow] refreshAllAccounts: Failed to select mailbox for:" << account.emailAddress;
            m_pythonEmailClient->disconnect();
            continue;
        }
        
        // 步骤3e：获取邮件列表
        QString jsonResult = m_pythonEmailClient->fetchEmailListAsJsonFast(1, 100);
        
        if (!jsonResult.isEmpty() && jsonResult != "{}" && jsonResult != "[]") {
            m_currentAccount = account;
            saveFetchedEmailsFromJson(jsonResult);
        }
        
        // 步骤3f：断开连接
        m_pythonEmailClient->disconnect();
        
        // 处理事件，避免UI冻结
        qApp->processEvents();
    }
    
    // 步骤4：合并所有账户的邮件到AllEmailBox
    m_progressDialog->setValue(90);
    m_progressDialog->setLabelText("正在合并邮件...");
    qApp->processEvents();
    
    bool mergeSuccess = m_emailDataManager->mergeAllEmailBoxesToAllEmailBox(m_accounts);
    
    if (!mergeSuccess) {
        qDebug() << "[EmailBoxWindow] refreshAllAccounts: Merge failed, using fallback";
    }
    
    // 步骤5：更新UI
    m_progressDialog->setValue(98);
    m_progressDialog->setLabelText("正在刷新界面...");
    qApp->processEvents();
    
    loadEmailsFromAllEmailBox();
    
    // 步骤6：清理
    m_progressDialog->setValue(100);
    cleanupProgressDialog();
    
    m_refreshBtn->setEnabled(true);
    m_refreshBtn->setText("刷新");
    
    qDebug() << "[EmailBoxWindow] refreshAllAccounts: Completed";
}
```

#### 3.2.6 修改 onAccountSelected() - 移除自动刷新

**当前代码位置**：约第760行

**操作步骤**：

1. 找到 `onAccountSelected()` 方法
2. 找到以下代码片段并修改

**找到的代码**：

```cpp
    m_currentAccount = m_accounts[accountIndex];

    if (m_pythonEmailClient->isConnected()) {
        m_pythonEmailClient->disconnect();
    }

    m_emailDataManager->clearEmailBoxForAccount(m_currentAccount.emailAddress);
    m_emailDataManager->createEmailBoxForAccount(m_currentAccount.emailAddress);

    fetchEmailsFromServer();
```

**替换为**：

```cpp
    m_currentAccount = m_accounts[accountIndex];

    // 断开之前的连接（如果有）
    if (m_pythonEmailClient->isConnected()) {
        m_pythonEmailClient->disconnect();
    }

    // 仅加载本地缓存，不触发网络请求
    loadLocalCachedEmails();
```

#### 3.2.7 删除废弃的函数（可选）

**操作步骤**：

以下函数可以删除或注释掉，因为已被新函数替代：

1. `fetchEmailsFromServer()` - 第330-430行（大约）
2. `performConnection()` - 第430-440行（大约）
3. `fetchEmailsForAllAccounts()` - 第260-330行（大约）

**建议**：先保留这些函数，确保程序正常运行后再逐步删除。

---

## 4. 辅助函数（无需修改）

以下函数保持原样，无需修改：

| 函数名 | 用途 | 状态 |
|--------|------|------|
| setupUI() | 初始化UI | 无需修改 |
| setupToolBar() | 创建工具栏 | 无需修改 |
| setupEmailListArea() | 创建邮件列表区域 | 无需修改 |
| loadEmails() | 加载邮件（旧方法，保留兼容） | 无需修改 |
| updateEmailList() | 更新邮件列表 | 无需修改 |
| saveFetchedEmails() | 保存邮件（旧方法） | 无需修改 |
| saveFetchedEmailsFromJson() | 从JSON保存邮件 | 无需修改 |
| displayEmails() | 显示邮件列表 | 无需修改 |
| cleanupProgressDialog() | 清理进度对话框 | 无需修改 |
| onImapConnected() | IMAP连接成功回调 | 无需修改 |
| onImapAuthenticated() | IMAP认证成功回调 | 无需修改 |
| onMailboxSelected() | 邮箱选择成功回调 | 无需修改 |
| onFetchProgress() | 获取进度回调 | 无需修改 |
| onImapError() | IMAP错误回调 | 无需修改 |
| onEmailListFetchedAsJson() | 邮件列表获取回调 | 无需修改 |
| onEmailViewClicked() | 邮件查看点击 | 无需修改 |
| onEmailDeleteClicked() | 邮件删除点击 | 无需修改 |
| onConnectionResult() | 连接结果回调 | 无需修改 |

---

## 5. 实施检查清单

### 5.1 EmailDataManager.h 修改清单

- [ ] 在 `private:` 之前添加了5个新方法声明
- [ ] 方法声明顺序：prepareEmailBoxForRefresh → loadEmailsFromEmailBox → prepareAllEmailBoxForRefresh → mergeAllEmailBoxesToAllEmailBox → loadEmailsFromAllEmailBox
- [ ] 添加了方法功能注释

### 5.2 EmailDataManager.cpp 修改清单

- [ ] 实现了 `prepareEmailBoxForRefresh()`
- [ ] 实现了 `loadEmailsFromEmailBox()`
- [ ] 实现了 `prepareAllEmailBoxForRefresh()`
- [ ] 实现了 `createAllEmailBoxDatabase()`
- [ ] 实现了 `mergeAllEmailBoxesToAllEmailBox()`
- [ ] 实现了 `loadEmailsFromAllEmailBox()`
- [ ] 每个方法都有日志输出
- [ ] 错误处理完善

### 5.3 EmailBoxWindow.h 修改清单

- [ ] 将 `fetchEmailsFromServer()` 替换为 `refreshSingleAccount(const EmailAccount& account)`
- [ ] 将 `fetchEmailsForAllAccounts()` 替换为 `refreshAllAccounts()`
- [ ] 添加了 `loadLocalCachedEmails()` 声明

### 5.4 EmailBoxWindow.cpp 修改清单

- [ ] 修改了 `showEvent()` - 移除自动刷新逻辑
- [ ] 添加了 `loadLocalCachedEmails()` 实现
- [ ] 修改了 `onRefreshEmails()` - 改为调用新的刷新方法
- [ ] 添加了 `refreshSingleAccount()` 实现
- [ ] 添加了 `refreshAllAccounts()` 实现
- [ ] 修改了 `onAccountSelected()` - 移除自动刷新

### 5.5 测试检查清单

- [ ] 首次启动邮箱界面不触发网络请求
- [ ] 点击刷新按钮可以正常刷新单账户邮件
- [ ] 点击刷新按钮可以正常刷新全部账户邮件
- [ ] 全部账户邮件按时间降序排列
- [ ] 刷新过程中可以取消
- [ ] 刷新失败后显示错误提示
- [ ] 刷新失败后仍能显示本地缓存邮件

---

## 6. 精确代码对照表

### 6.1 EmailBoxWindow.h 变更详情

| 行号范围 | 修改类型 | 描述 |
|---------|---------|------|
| 74-78 | 删除 | `fetchEmailsFromServer(); performConnection();` |
| 74-78 | 添加 | `refreshSingleAccount(const EmailAccount& account);` |
| 74-78 | 添加 | `refreshAllAccounts();` |
| 74-78 | 添加 | `loadLocalCachedEmails();` |

### 6.2 EmailBoxWindow.cpp 变更详情

| 函数名 | 原位置 | 修改类型 | 新行为 |
|--------|--------|---------|--------|
| showEvent() | 107-119行 | 完全替换 | 仅加载本地缓存，不自动刷新 |
| onRefreshEmails() | 243-260行 | 完全替换 | 根据账户类型调用新方法 |
| onAccountSelected() | 760行附近 | 部分修改 | 移除 fetchEmailsFromServer 调用 |
| loadLocalCachedEmails() | 新增 | 新增 | 加载本地缓存邮件 |
| refreshSingleAccount() | 新增 | 新增 | 单账户刷新流程 |
| refreshAllAccounts() | 新增 | 新增 | 全部账户刷新流程 |

### 6.3 EmailDataManager.h 变更详情

| 新增方法 | 位置 | 功能 |
|---------|------|------|
| prepareEmailBoxForRefresh() | private之前 | 准备单账户EmailBox |
| loadEmailsFromEmailBox() | private之前 | 从EmailBox加载邮件 |
| prepareAllEmailBoxForRefresh() | private之前 | 准备AllEmailBox |
| mergeAllEmailBoxesToAllEmailBox() | private之前 | 合并所有账户邮件 |
| loadEmailsFromAllEmailBox() | private之前 | 从AllEmailBox加载邮件 |

### 6.4 EmailDataManager.cpp 变更详情

| 新增方法 | 功能 | 关键步骤 |
|---------|------|----------|
| prepareEmailBoxForRefresh() | 清空并创建EmailBox | 调用clear + create |
| loadEmailsFromEmailBox() | 加载指定账户邮件 | 打开db → 查询 → 解析 → 关闭 |
| prepareAllEmailBoxForRefresh() | 清空并创建AllEmailBox | 删除文件 → 创建新db |
| createAllEmailBoxDatabase() | 创建AllEmailBox结构 | 创建表 + 索引 |
| mergeAllEmailBoxesToAllEmailBox() | 合并所有邮件 | 遍历 → 插入 → 排序 |
| loadEmailsFromAllEmailBox() | 加载AllEmailBox邮件 | 打开db → 查询 → 解析 → 关闭 |

---

## 7. 依赖关系说明

### 7.1 EmailBoxWindow 依赖关系

```
showEvent()
    └── loadLocalCachedEmails()
            ├── m_emailDataManager->loadEmailsFromAllEmailBox()
            └── m_emailDataManager->loadEmailsFromEmailBox()

onRefreshEmails()
    ├── refreshSingleAccount()     (当 m_currentAccount.id != 0)
    │       ├── m_emailDataManager->prepareEmailBoxForRefresh()
    │       ├── m_pythonEmailClient->connectToServer()
    │       ├── m_pythonEmailClient->selectMailbox()
    │       ├── m_pythonEmailClient->fetchEmailListAsJsonFast()
    │       └── saveFetchedEmailsFromJson()
    └── refreshAllAccounts()       (当 m_currentAccount.id == 0)
            ├── m_emailDataManager->prepareEmailBoxForRefresh() (循环)
            ├── m_pythonEmailClient->connectToServer() (循环)
            ├── m_pythonEmailClient->fetchEmailListAsJsonFast() (循环)
            ├── saveFetchedEmailsFromJson() (循环)
            ├── m_emailDataManager->mergeAllEmailBoxesToAllEmailBox()
            └── loadEmailsFromAllEmailBox()
```

### 7.2 EmailDataManager 依赖关系

```
prepareEmailBoxForRefresh()
    ├── clearEmailBoxForAccount() (已有)
    └── createEmailBoxForAccount() (已有)

prepareAllEmailBoxForRefresh()
    └── createAllEmailBoxDatabase()

createAllEmailBoxDatabase()
    └── 创建数据库表结构

mergeAllEmailBoxesToAllEmailBox()
    ├── prepareAllEmailBoxForRefresh()
    └── loadEmailsFromEmailBox() (循环)

loadEmailsFromAllEmailBox()
    └── 查询数据库

loadEmailsFromEmailBox()
    └── 查询数据库
```

---

## 8. 错误处理策略

### 8.1 网络错误

**场景**：连接服务器失败

**处理**：

1. 显示错误消息对话框
2. 断开连接
3. 加载本地缓存邮件
4. 记录日志

### 8.2 数据库错误

**场景**：无法打开数据库

**处理**：

1. 记录错误日志
2. 返回空列表
3. UI显示空状态

### 8.3 用户取消

**场景**：用户在进度对话框中点击取消

**处理**：

1. 立即停止刷新循环
2. 显示已获取的邮件
3. 恢复刷新按钮状态
4. 记录日志

---

## 9. 性能考虑

### 9.1 批量操作

- 合并邮件时使用事务
- 减少数据库提交次数
- 提高大数据量处理性能

### 9.2 异步处理

- 网络请求在独立线程执行
- UI保持响应
- 使用 QTimer::singleShot 避免阻塞

### 9.3 数据限制

- 单次获取最多100封邮件
- 显示最多12封邮件
- 定期清理过期邮件（后续优化）

---

## 10. 后续优化建议

### 10.1 短期优化（可在本次重构后实施）

1. 添加邮件获取数量的配置选项
2. 支持增量更新（而非全量刷新）
3. 添加最后刷新时间显示

### 10.2 长期优化（可在后续版本实施）

1. 定时自动刷新（可配置间隔）
2. 离线模式支持
3. 邮件同步状态监控
4. 分布式邮件获取优化

---

**文档版本**：2.0  
**更新日期**：2026-04-16  
**审核状态**：待审核
